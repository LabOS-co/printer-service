# Real secrets and per-machine backend credentials. Generated by
# scripts/bootstrap.sh; never commit a filled-in copy.
.env.local

# Newman JSON reports, one per run.
reports/

# Generated fixtures. Reproducible from scripts/make-fixtures.sh, and
# printDemo.pdf is a 2.5 MB copy of a file already tracked at the repo root.
testdata/

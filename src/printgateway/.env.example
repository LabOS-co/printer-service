# Copy to .env (same directory, next to docker-compose.yml) and fill in a
# real value — never commit a filled-in copy. docker compose reads .env
# automatically; it is git-ignored (see .gitignore).
PRINT_GATEWAY_TOKEN=
